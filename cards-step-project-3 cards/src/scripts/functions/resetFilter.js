const resetFilter = () => {
    document.querySelector("._doctor-filter").value = ``
    document.querySelector("._priority-filter").value = ``
    document.querySelector("._text-filter").value = ``
}

export default resetFilter